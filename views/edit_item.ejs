<!DOCTYPE html>
<html>
<head>
    <title>Изменить товар</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <header>
        <nav>
            <ul>
                <li><a href="/">Главная</a></li>
                <li><a href="/news">Новости</a></li>
                <li><a href="/courses">Курсы</a></li>
                <li><a href="/store">Магазин</a></li>
                <li><a href="/reviews">Отзывы</a></li>
            </ul>
        </nav>
    </header>
    <main class="main-form">
        <h1>Изменить товар</h1>
        <form action="/store/edit/<%= item.id %>" method="post" enctype="multipart/form-data">
            <label for="title">Название товара:</label>
            <input type="text" id="title" name="title" value="<%= item.item_name %>" required><br>

            <label for="description">Описание товара:</label>
            <textarea id="description" name="description" required><%= item.item_description %></textarea><br>

            <label for="price">Цена:</label>
            <input type="number" id="price" name="price" value="<%= item.item_price %>" required><br>

            <label for="image">Новое изображение товара:</label>
            <input type="file" id="image" name="image"><br>

            <% if (images.length > 0) { %>
                <h3>Существующие изображения:</h3>
                <ul>
                    <% images.forEach(image => { %>
                        <li>
                            <img src="/uploads/<%= image.image_url %>" alt="Product Image" style="max-width: 300px; max-height: 200px;"><br>
                            <a href="/uploads/<%= image.image_url %>" download>Скачать</a>
                            <form action="/store/delete-image/<%= image.id %>" method="post" style="display:inline;">
                                <button type="submit" class="btn-delete-file" onclick="return confirm('Вы уверены, что хотите удалить это изображение?');">Удалить</button>
                            </form>
                        </li>
                    <% }); %>
                </ul>
            <% } %>

            <label for="additional_images">Дополнительные изображения:</label>
            <input type="file" id="additional_images" name="additional_images" multiple><br>

            <button type="submit" class="btn-submit">Сохранить изменения</button>
            <a href="/store" class="btn-cancel">Отмена</a>
        </form>
    </main>
</body>
</html>
